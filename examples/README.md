# TypeScript ESM Hello World Example

This example demonstrates a minimal TypeScript file using ESM/MJS-style module semantics via the `.mts` extension.

## Run

From the project root:

```sh
npx tsx examples/hello-world.mts
```

Expected output:

```text
Hello, World!
```
